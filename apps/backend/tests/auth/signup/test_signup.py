#!/usr/bin/env python3
"""Placeholder test for signup. Replace with real API calls (e.g. httpx) when ready."""
import os
import sys
BASE = os.environ.get("API_BASE_URL", "http://localhost:8000")
# TODO: POST BASE + "/api/auth/signup" with email, password; assert 200 and gitea/supabase success
sys.exit(0)
