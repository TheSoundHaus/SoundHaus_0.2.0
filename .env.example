# =============================================================================
# SoundHaus Docker Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env files with real credentials to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Settings → Database
# Connection string format: postgres://[USER]:[PASSWORD]@[HOST]:[PORT]/[NAME]

SUPABASE_DB_HOST=db.xxxxxxxxxxxxxx.supabase.co
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=postgres
SUPABASE_DB_PASSWORD=your-database-password-here
SUPABASE_DB_SSL_MODE=require

# -----------------------------------------------------------------------------
# Supabase API Configuration
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Settings → API

SUPABASE_URL=https://xxxxxxxxxxxxxx.supabase.co
SUPABASE_PUB_KEY=your-anon-public-key-here
SUPABASE_SERVICE_KEY=your-service-role-key-here
SUPABASE_JWT_SECRET=your-jwt-secret-here

# -----------------------------------------------------------------------------
# Gitea Admin Token
# -----------------------------------------------------------------------------
# Generate after first Gitea setup:
# 1. Access Gitea at http://localhost:3000 (or your domain)
# 2. Login with admin account
# 3. Go to: Settings → Applications → Manage Access Tokens
# 4. Generate new token with ALL scopes (especially write:admin)
# 5. Copy token here

GITEA_ADMIN_TOKEN=your-gitea-admin-token-here

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Local development:
#   ROOT_URL=http://localhost:3000/
#   DOMAIN=localhost
#
# Production (Digital Ocean):
#   ROOT_URL=http://YOUR_DROPLET_IP:3000/
#   DOMAIN=YOUR_DROPLET_IP

ROOT_URL=http://localhost:3000/
DOMAIN=localhost

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# Digital Ocean Spaces Configuration
# -----------------------------------------------------------------------------
# For LFS storage of large audio files
# Get these from: Digital Ocean Dashboard → API → Spaces access keys
# Endpoint format: https://[REGION].digitaloceanspaces.com

DO_SPACES_ENDPOINT=nyc3.digitaloceanspaces.com
DO_SPACES_KEY=your-spaces-access-key-here
DO_SPACES_SECRET=your-spaces-secret-key-here
DO_SPACES_BUCKET=soundhaus-lfs
DO_SPACES_REGION=nyc3
