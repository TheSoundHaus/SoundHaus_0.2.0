# =============================================================================
# Docker Compose variable substitution
# =============================================================================
# This file is a LEGACY fallback. Prefer:
#   .env.compose.local  (local development)
#   .env.compose.remote (production / DigitalOcean)
#
# Copy to .env if you need a quick-start default:
#   cp .env.example .env
#
# These vars are used by docker-compose.yml for ${VAR} interpolation.
# Backend app config lives in apps/backend/.env.local or .env.remote.
# =============================================================================

# Gitea Docker services (gitea_db + gitea)
# Generate secure values:
#   openssl rand -hex 32                              (for GITEA_DB_PASSWORD)
#   docker run --rm gitea/gitea:1.24.6 gitea generate secret SECRET_KEY      (for GITEA_SECRET_KEY)
#   docker run --rm gitea/gitea:1.24.6 gitea generate secret INTERNAL_TOKEN  (for GITEA_INTERNAL_TOKEN)
GITEA_DB_PASSWORD=your-gitea-db-password
GITEA_SECRET_KEY=your-gitea-secret-key
GITEA_INTERNAL_TOKEN=your-gitea-internal-token

# Backend env file path (used by docker-compose fastapi env_file)
BACKEND_ENV_FILE=./apps/backend/.env.local

# Images / runtime profile
TAG=latest
ENVIRONMENT=development
LOG_LEVEL=INFO
LOG_FORMAT=auto

# Token Broker settings
GITEA_CONTAINER_NAME=gitea
DOCKER_EXEC_TIMEOUT_SECONDS=15
BROKER_API_KEY=

# Host home directory (SSH key volume mount)
# macOS: /Users/YourUsername   Linux: /home/username   Windows: C:\Users\YourUsername
HOME=/Users/YourUsername
# 5. Copy to apps/backend/.env as GITEA_ADMIN_TOKEN

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
API_URL=http://localhost:8000
